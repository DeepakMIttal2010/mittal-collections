# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# sitemap.xml is served dynamically by api/sitemap.js (see vercel.json's
# rewrite) — nothing generates a static copy here anymore, this just
# guards against a stray local one shadowing the rewrite (see api/sitemap.js)
public/sitemap.xml

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
